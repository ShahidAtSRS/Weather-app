# Weather-app
A polished, self-contained weather app. It is using the Open-Meteo API (free, no API key needed) combined with the Open-Meteo Geocoding API — so users can just type any city name with zero setup.
